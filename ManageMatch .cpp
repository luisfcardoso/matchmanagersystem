#include <string>
#include <vector>
#include <list>
#include <iostream>
#include <assert.h>

#include "ManageMatch .h"


string ManageMatch ::addMatch(string name, Date time, float ticketPrice, string stadiumName, string city, string state, MatchManager owner, int matchType, string teams[], int numberOfTickets)
{
	return 0;
}

string ManageMatch ::updateMatch(int matchID, string parameter, string value)
{
	return 0;
}

void ManageMatch ::removeMatch(int matchID)
{
}
